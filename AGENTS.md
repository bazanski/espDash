# Project-level Opencode Instructions for espDash

- **Startup Checklist**: Always pull the latest project state on start:
  ```bash
  python3 ~/.agentsync/agentsync.py pull
  ```
- **Turn-Completion**: Always sync updates back to the tracker before finishing:
  ```bash
  python3 ~/.agentsync/agentsync.py sync
  ```
